# Using OpenCV to manipulate images

## Description

Get a live feed of the camera and manipulate the frames with changing the resolution and scaling it. Save the manipulated image in the directory
